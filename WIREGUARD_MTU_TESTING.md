# WireGuard MTU 原理与实战测试指南（ICMP Fragmentation Needed 验证）

本文档详细介绍 WireGuard 接口 MTU（最大传输单元）的计算原理，以及如何使用简单的 `ping` 命令结合“禁止分片”（DF, Don't Fragment）标志位，在实际网络环境中触发并验证内核返回的 **`Frag needed and DF set (mtu = 1420)`**（ICMP Type 3, Code 4 报文）。

---

## 一、核心概念辨析与报文计算

### 1. 极易混淆的概念辨析：MTU 与 MSS 到底差了什么？

在排查网络分片与黑洞问题时，许多人对各种报头大小和换算公式感到困惑。理清以下三个核心概念是准确计算数据包大小的前提：

#### ① MSS 包不包含 TCP 头部或 UDP 头部？
- **MSS 绝对不包含 TCP 头部**：MSS（Maximum Segment Size，最大报文段大小）在 RFC 793 / RFC 879 中被严格定义为 **TCP 报文段中能够承载的“纯应用层数据载荷（Payload）”的最大字节数**。它既不包含 TCP 头部，也不包含 IP 头部。
- **UDP 根本没有 MSS 概念**：MSS 是 TCP 协议特有的参数，仅在 TCP 三次握手（SYN 包 Options）时由两端协商。UDP 是面向无连接的数据报协议，没有握手机制，应用程序想发多大就直接下发给内核，超限则由底层 IP 层分片或被丢弃。

#### ② MTU 与 MSS 到底相差什么？
MTU 和 MSS 之间**不是只差一个 IP 头部，而是差了【IP 头部 + TCP 头部】两个头部**！

```text
|<----------------------------- MTU (以太网最大传输单元) ----------------------------->|
+--------------------------+--------------------------+----------------------------+
|      IP 头部 (网络层)     |     TCP 头部 (传输层)     |     TCP 纯应用层载荷数据     |
|  IPv4: 20B / IPv6: 40B   |       固定基础: 20B       |           (即 MSS)         |
+--------------------------+--------------------------+----------------------------+
                                                      |<---------- MSS ----------->|
```

由此得出严格的数学换算公式：

$$\mathbf{MSS = MTU - IP头部大小 - TCP头部大小}$$

#### ③ IP 头部与 TCP 头部的真实大小
- **IPv4 头部**：基础长度固定为 **20 字节**（首部长度字段 IHL 虽支持扩展到 60 字节，但在现代互联网中 Options 选项极少使用，通常均固定为 20 字节）。
- **IPv6 头部**：去掉了 IPv4 的繁杂选项，基础固定头部严格为 **40 字节**。
- **TCP 头部**：基础长度固定为 **20 字节**（包含源/目的端口、序号、确认号、Flags 标志位等）。

#### ④ 常见网络环境的数值对照表

| 场景环境 | 接口 MTU | IP 头部 | TCP 头部 | 最终协商 MSS | 计算推导公式 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **标准以太网 (IPv4)** | 1500 字节 | 20 字节 | 20 字节 | **1460 字节** | $1500 - 20 - 20 = 1460$（互联网最常见默认值） |
| **标准以太网 (IPv6)** | 1500 字节 | 40 字节 | 20 字节 | **1440 字节** | $1500 - 40 - 20 = 1440$ |
| **WireGuard 隧道 (IPv4 业务)** | 1420 字节 | 20 字节 | 20 字节 | **1380 字节** | $1420 - 20 - 20 = 1380$（TCPMSS 自动钳制的目标值） |

---

### 2. 为什么 WireGuard 默认 MTU 是 1420？

在标准的物理以太网中，数据帧的 MTU 通常为 **1500 字节**。当 WireGuard 在 Underlay（底层网络）传输数据时，需要对原始数据包进行加密并添加外层报头封装。

WireGuard 的外层封装开销共计 **80 字节**（以 IPv6 Underlay 为准）：

| 协议层 | 报文组件 | 占用长度 | 说明 |
| :--- | :--- | :--- | :--- |
| **外层 IP 报头** | IPv6 Header | **40 字节** | 若 Underlay 为 IPv4 则为 20 字节；WireGuard 驱动默认保守预留 40 字节 |
| **外层 UDP 报头** | UDP Header | **8 字节** | 包含源端口、目的端口、长度和校验和 |
| **WireGuard 协议头** | Data Header | **16 字节** | 报文类型（Type 1B + Reserved 3B = 4B）、接收方索引（4B）、计数器/Nonce（8B） |
| **WireGuard 认证尾** | Poly1305 MAC | **16 字节** | AEAD 加密认证标签，用于防篡改与完整性校验 |
| **总开销 (Overhead)** | — | **80 字节** | $40 + 8 + 16 + 16 = 80$ |

因此，WireGuard 虚拟接口（`wg0`）的默认 MTU 计算为：

$$\text{WireGuard MTU} = \text{物理网卡 MTU} - \text{封装开销} = 1500 - 80 = \mathbf{1420}$$

---

### 3. `ping` 命令的报文尺寸计算与临界点

在 Linux 中，`ping -s <包大小>` 指定的是 **ICMP 载荷（Payload）大小**，而不是最终的整个 IP 报文总长度。

网络层传输的完整 IPv4 数据包大小公式为：

$$\text{IPv4 数据包总长} = \text{ICMP 载荷} + \text{ICMP 头部 (8 字节)} + \text{IPv4 头部 (20 字节)} = \text{载荷} + \mathbf{28 \text{ 字节}}$$

对于 MTU 为 **1420** 的 WireGuard 隧道：

| ICMP 载荷 (`-s`) | 头部开销 | 最终 IP 报文大小 | 状态 | 预期网络行为 |
| :--- | :--- | :--- | :--- | :--- |
| **1392** | 28 字节 | **1420 字节** | 刚好等于 MTU | 正常通行，正常 Ping 通 |
| **1393** | 28 字节 | **1421 字节** | 超出 MTU 1 字节 | **触发超限**，网关回送 ICMP 报错 |
| **1400** | 28 字节 | **1428 字节** | 超出 MTU 8 字节 | **典型超限测试包**，稳定触发 ICMP 报错 |

---

## 二、实战测试步骤

### 1. 测试节点选择（关键要点）

> [!IMPORTANT]
> **测试命令必须在 VPC 子网内的测试机上执行，而不是直接在网关上执行**：
> - **测试机（MTU 1500）**：物理网卡允许发出 1428 字节的大包。数据包经过 VPC 路由表转发到达网关时，网关发现下一跳接口 `wg0`（MTU 1420）容纳不下且带了 DF 标志，便会**作为中间路由器丢弃该包并向源端回送 ICMP 错误报文**。
> - **如果在网关本机执行**：网关本地 Socket 在构造报文阶段就已知晓 `wg0` 的 MTU 是 1420，内核会直接在协议栈内部阻断（报错 `ping: local error: message too long`），无法模拟真实的跨路由器 PMTU 协商过程。

---

### 2. 第一步：正常通行基准测试（载荷 1392 字节）

登录到**杭州测试机**，向**上海测试机**（例如 `192.168.20.x`）发起 ping，设置 `-M do`（开启 DF 禁分片）和 `-s 1392`：

```bash
# -c 2: 发送2个包
# -M do: 开启 Don't Fragment (DF=1)
# -s 1392: 载荷 1392 字节 (总长 1392 + 28 = 1420 字节)
ping -c 2 -M do -s 1392 192.168.20.x
```

**终端预期输出**：
```text
PING 192.168.20.x (192.168.20.x) 1392(1420) bytes of data.
1400 bytes from 192.168.20.x: icmp_seq=1 ttl=63 time=28.4 ms
1400 bytes from 192.168.20.x: icmp_seq=2 ttl=63 time=28.1 ms

--- 192.168.20.x ping statistics ---
2 packets transmitted, 2 received, 0% packet loss, time 1001ms
rtt min/avg/max/mdev = 28.142/28.285/28.428/0.143 ms
```
*解释：总长刚好为 1420 字节，与 `wg0` 接口 MTU 完全吻合，报文无缝经由 WireGuard 封装并解封装送达对端。*

---

### 3. 第二步：超限触发测试（载荷 1400 字节）

将载荷增大至 **1400 字节**（总长 $1400 + 28 = 1428 > 1420$ 字节）：

```bash
# 载荷 1400 字节 (总长 1428 字节 > 1420 字节)
ping -c 2 -M do -s 1400 192.168.20.x
```

**终端预期输出**：
```text
PING 192.168.20.x (192.168.20.x) 1400(1428) bytes of data.
From 192.168.10.251 icmp_seq=1 Frag needed and DF set (mtu = 1420)
From 192.168.10.251 icmp_seq=2 Frag needed and DF set (mtu = 1420)

--- 192.168.20.x ping statistics ---
2 packets transmitted, 0 received, +2 errors, 100% packet loss, time 1002ms
```

**输出解读**：
1. **报错来源**：回包来自 `192.168.10.251`（即**杭州 WireGuard 网关的私网 IP**），证明报错确实由下一跳中间网关触发。
2. **错误描述**：明确提示 **`Frag needed and DF set`**（需要分片但设置了禁止分片标志）。
3. **关键参数反馈**：附带告知了该路径所允许的实际 **`(mtu = 1420)`**。

---

## 三、使用 `tcpdump` 抓包深度验证

为了观察网络包交互全貌，可以在 **杭州 WireGuard 网关 ECS** 上新开一个终端窗口运行抓包：

```bash
# 抓取流经网关所有网卡的 ICMP 报文
tcpdump -n -i any icmp
```

此时在测试机上再次执行 `ping -c 1 -M do -s 1400 192.168.20.x`，网关侧抓包终端会实时打印出如下成对报文：

```text
10:50:12.345678 eth0  In  IP 192.168.10.15 > 192.168.20.88: ICMP echo request, id 5678, seq 1, length 1408
10:50:12.345710 eth0  Out IP 192.168.10.251 > 192.168.10.15: ICMP 192.168.20.88 unreachable - need to frag (mtu 1420), length 556
```

**报文流向解析**：
1. **第 1 行**：网关的 `eth0` 网卡收到来自测试机 `192.168.10.15`、目的地址为 `192.168.20.88` 的超大 Echo Request（IP 总长为 1428 字节）。
2. **中间处理**：网关内核路由判决出接口为 `wg0`，检查发现 `wg0` MTU 为 1420，而报文携带 DF 标志不可分片，触发丢包。
3. **第 2 行**：网关协议栈立即从 `eth0` 向源测试机 `192.168.10.15` 发出 ICMP Type 3 Code 4 报错：`unreachable - need to frag (mtu 1420)`。

---

## 四、生产环境避坑：路径 MTU 黑洞与 TCP MSS 钳制

### 1. 什么是“路径 MTU 黑洞”（PMTU Black Hole）？
在理想网络中，发送端收到上述 `Frag needed (mtu 1420)` 报文后，会自动调小其发送的 TCP 数据段大小（MSS）。

然而在跨公网、多运营商或混合云环境中，**中间链路的防火墙或安全设备经常会错误地将所有 ICMP 报文全部阻断**。这会导致：
- 测试机发送大包被网关丢弃；
- 网关回复的 ICMP 错误报文被中间设备丢弃，测试机收不到通知；
- 测试机持续超时重传大包，陷入无限等待。

**典型表象**：
- 小包通信正常：`ping` 和 SSH 登录能够秒连；
- 大包通信卡死：执行 `curl` 获取大网页、`scp` 传输文件、`git clone` 或拉取镜像时**连接突然挂起、停滞无响应**。

---

### 2. 根治方案：在 WireGuard 网关配置 TCP MSS 自动钳制

在 WireGuard 网关节点上配置一条 `iptables` 规则，对所有经由网关转发的 TCP 握手报文进行 MSS 钳制：

```bash
# 自动将 TCP MSS 协商值钳制在当前路径的有效 MTU 范围内
iptables -t mangle -A FORWARD -p tcp --tcp-flags SYN,RST SYN -j TCPMSS --clamp-mss-to-pmtu
```

**原理解析**：
- 当客户端发起 TCP 连接发送 `SYN` 握手包时，网关在 `mangle` 链拦截该包；
- 规则自动将包内的 TCP MSS 通告值重写为不超过 $1420 - 40 = 1380$ 字节；
- 双方协商出的 MSS 将严格适配 WireGuard 隧道，从源头上避免产生超过 1420 字节的 IP 报文，彻底免疫“ICMP 报文丢失”带来的黑洞问题。

---

## 五、实战经典案例：PostgreSQL 跨云连通但查询死锁故障复盘

### 1. 故障现象与极高迷惑性

在混合云或多云架构中，常常发生以下极具迷惑性的典型故障：
- **拓扑**：PostgreSQL 数据库部署在云厂商 A，应用/客户端在云厂商 B，中间使用 WireGuard 隧道打通私网通信；
- **排查表现**：
  1. `ping` 数据库私网 IP：**全部秒通，0% 丢包，延时稳定**；
  2. `ssh` 登录对端机器：**秒连，敲命令（`ls`、`pwd`、`uptime`）流畅无阻**；
  3. `psql -h <PG_IP> -U postgres`：**连接成功，顺利进入 `mydb=> ` 交互命令行**；
  4. **致命故障**：只要在交互命令行敲下任何查询命令（如 `SELECT * FROM large_table;` 甚至是 `\d` 查看表定义），终端光标便**无限期停滞闪烁，既不报错也不超时，整条连接彻底假死**。

---

### 2. 底层数据流时序与黑洞剖析

```text
客户端 (Client)                                   服务端 (PostgreSQL)
  |                                                        |
  |-------- 1. TCP 三次握手 (SYN: 宣称本地 MSS=1460) ------>|  (顺利通行，小包 ~60B)
  |<------- 2. TCP 三次握手 (SYN-ACK: 宣称本地 MSS=1460) ---|  (顺利通行，小包 ~60B)
  |                                                        |
  |-------- 3. PG 认证报文 (StartupMessage / Password) --->|  (几百字节，顺利通行)
  |<------- 4. PG 准备就绪 (AuthenticationOk / Ready) -----|  (顺利通行，psql 显示提示符)
  |                                                        |
  |-------- 5. 执行查询: "SELECT * FROM my_table;" ------->|  (SQL 语句几十字节，顺利送达)
  |                                                        |
  |                                            [服务端执行查询，生成大结果集]
  |                                            [服务端按 MSS=1460 切成 1500B 大包]
  |                                            [报头携带 DF=1 (禁止分片)]
  |                                                        |
  |                           [WireGuard 接口 (MTU 1420)]   |
  |                                      |                 |
  |                                      x <=== 1500B 大包撞墙被强制丢弃!
  |                                      |
  |             [跨云公网防火墙拦截 ICMP] <--- [网关发送 ICMP Type 3 Code 4]
  |                        |
  |               (ICMP 报错丢失!)
  |
  | <=== 客户端在等待数据 (recv 阻塞挂起)       服务端在等待 ACK (超时重传 1500B 大包) ===>
```

#### 过程分步拆解：
1. **握手时埋下隐患**：
   - 客户端与服务端的物理网卡 MTU 均为 1500。
   - 在 TCP 三次握手（`SYN` / `SYN-ACK`）时，双方通告的最大段大小均为 $\text{MSS} = 1500 - 40 = \mathbf{1460}$ 字节。
   - 此时通信双方协议栈**完全感知不到中间存在 MTU 为 1420 的 WireGuard 隧道**。
2. **建连认证小包放行**：
   - 握手、SSL 协商、账号密码认证以及 `AuthenticationOk` 报文通常只有几百字节，远低于 1420，轻松穿透隧道到达客户端，形成“网络已通”的假象。
3. **大查询触发 1500 字节满载包**：
   - 用户敲入 `SELECT * FROM users;`。查询命令本身很短（几十字节），顺利送达 PG 服务端。
   - 服务端查询出多行数据，协议栈按照协商好的 `MSS = 1460` 组装满载的 TCP 数据包，加上 40 字节 IP/TCP 报头后成为标准的 **1500 字节 IP 数据包**，且默认携带 `DF=1`（禁止分片）。
4. **WireGuard 网关超限丢弃**：
   - 1500 字节的大包到达 WireGuard 网关准备进入 `wg0`，网关发现 $1500 > 1420$ 且标记了不可分片，只能**立即丢弃该包**。
5. **ICMP 差错报文被跨云丢弃（黑洞形成）**：
   - 网关向服务端回送 `ICMP Type 3, Code 4 (Fragmentation Needed, MTU=1420)`。
   - 但在跨云传输链路中，云厂商的安全组策略或公网防护设备通常**默认将所有 ICMP 报文视作非安全流量直接过滤**。
   - 服务端收不到 ICMP 报错，只以为网络发生偶然丢包，于是触发 TCP 指数退避超时重传，周而复始地重发那个 1500 字节的大包。
6. **客户端永久挂起**：
   - 客户端应用由于未收到查询返回数据，处于系统调用 `recv()` 的无限阻塞状态，现象就是客户端光标闪烁无任何响应。

---

### 3. 故障快速定位三板斧

如果你在任何环境遇到“**能建连、握手正常，但执行查询/拉取文件假死**”的现象，用以下三步可以在 30 秒内完成定障：

1. **第一斧：用带 DF 的大 ping 快速验证**：
   ```bash
   # 直接发送一个 1400 字节有效载荷（总长 1428 字节）的禁分片包
   ping -c 2 -M do -s 1400 <目标数据库IP>
   ```
   *若出现 `Frag needed and DF set (mtu = 1420)` 或 100% 丢包，即可 100% 判定为 MTU 黑洞。*

2. **第二斧：在 SSH 里输出大文本验证**：
   ```bash
   # 在 SSH 会话中故意输出大量文本
   dmesg
   # 或者
   cat /var/log/messages
   ```
   *若敲小命令正常，一执行 `dmesg` 整个 SSH 终端也立刻假死，说明整条隧道均存在大包受阻问题。*

3. **第三斧：在网关上抓 TCP 重传**：
   ```bash
   tcpdump -n -i wg0 'tcp[tcpflags] & (tcp-syn) == 0 and length > 1400'
   ```
   *若能抓到服务端持续不断重传同一长度大于 1400 的 TCP 包，而客户端始终无 ACK，说明大包正在被静默丢弃。*

---

### 4. 针对该案例的根治方案

在负责互联的 WireGuard 网关上增加 **TCP MSS 自动钳制** 规则（推荐写在网关的 `PostUp` 或防火墙初始化脚本中）：

```bash
iptables -t mangle -A FORWARD -p tcp --tcp-flags SYN,RST SYN -j TCPMSS --clamp-mss-to-pmtu
```

**生效机制**：
- 网关在转发握手包 `SYN` 时，自动拦截并将两端声明的 MSS 强行修改为不超过 $1420 - 40 = 1380$ 字节；
- 数据库服务端在发送任何查询结果时，单包最大尺寸绝不会超过 1420 字节；
- 彻底绕过对链路中 ICMP 报错通道的依赖，一劳永逸杜绝此类假死问题。

---

## 六、各操作系统 Ping 命令参数速查表

不同操作系统下指定 DF 标志与载荷大小的参数略有差异：

| 操作系统 | 开启 DF 禁分片标志 | 指定载荷大小 (字节) | 触发 1420 MTU 报错的测试命令 |
| :--- | :--- | :--- | :--- |
| **Linux** | `-M do` | `-s <size>` | `ping -M do -s 1400 <目标IP>` |
| **macOS** | `-D` | `-s <size>` | `ping -D -s 1400 <目标IP>` |
| **Windows** | `-f` | `-l <size>` | `ping -f -l 1400 <目标IP>` |

